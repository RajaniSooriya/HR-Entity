# +required Version of the endpoint configuration YAML
version: 0.1

# +required List of endpoints to create
endpoints:
- name: HR Entity
  port: 9090
  type: GraphQL
  visibility: Public
  context: /
